#include <iostream>
using namespace std;

void fast() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
}
int main() {
	fast();
	int t;
	cin>>t;
	while(t--) {
		int n,x,d;
		cin>>n>>x>>d;
		int org=n/(x*5);
		int final=org+d;
		cout<<final<<endl;
	}
}
